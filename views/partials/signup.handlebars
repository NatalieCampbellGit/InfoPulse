<!-- Sign up Form-->
<div class="col-lg-6">
      <div class="card">
        <div class="card-header bg-success-subtle text-dark">
          <h3 class="card-title">Sign Up</h3>
        </div>
        <div class="card-body">
          <p>If you have been given an authentication code, enter your passcode and email below, then choose a username and password and enter it below.</p>
          <form id="signup-form">
            <div class="form-group form-check">
              <input type="checkbox" class="form-check-input" id="generate-passcode" name="generate-passcode">
              <label class="form-check-label" for="generate-passcode">One-Time Passcode</label>
            </div>
            <div class="form-group">
              <label for="email">email</label>
              <input type="email" class="form-control" id="email" name="email" autocomplete="off">
            </div>
            <div class="form-group">
              <label for="signup-username">username</label>
              <input type="text" class="form-control" id="signup-username" name="signup-username" autocomplete="off">
            </div>
            <div class="form-group">
              <label for="signup-password">Password</label>
              <input type="password" class="form-control" id="signup-password" name="signup-password" autocomplete="off">
            </div>
            <div class="form-group">
              <label for="confirm-password">Confirm password</label>
              <input type="password" class="form-control" id="confirm-password" name="confirm-password" autocomplete="off">
            </div>
            <button type="submit" id="sign-up-button" class="btn btn-success mt-1">Sign up</button>
          </form>
        </div>
        <div class="card-footer">
          <small class="text-muted">Already have an account? <a href="/login">Login here</a></small>
        </div>
      </div>
    </div>

<!-- signup script -->
<script src="/assets/js/signup.js"></script>

<!-- {{> signup}}
  Login Forms -->
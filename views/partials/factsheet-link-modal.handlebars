{{! a modal for asking an
administrator to add custom markdown to a selected
factsheet for the user }}

<!-- Modal for inserting a custom markdown -->
<div
  class="modal-content hidden w-full rounded-md shadow-lg sm:max-w-screen-sm"
>
  <div class="text-center">
    <h3 class="text-2xl font-bold text-pulse-blue-500">Link Factsheet to Client</h3>
    <p id="modal-instructions" class="text-sm text-pulse-blue-500">Enter a
      personalised message to your client in markdown format. Press Save to
      confirm the linking of the factsheet to the selected client.</p>
  </div>
  <div class="flex flex-col space-y-2">
    <div class="flex text-center">
      <p class="text-sm text-pulse-blue-500">User:
        {{user.last_name}},
        {{user.first_name}}</p>
      <p class="text-sm text-pulse-lt-blue-500">Enter personalised markdown text
        for:</p>
      <p class="text-base text-pulse-lt-blue-500">{{template.title}}</p>
    </div>
    <div>
      <textarea
        class="w-full rounded-md border border-pulse-green-600 bg-white p-2 outline-none focus:ring-2 focus:ring-pulse-green-500"
        id="custom-markdown"
        rows="40"
        wrap="soft"
        cols="100"
      ></textarea>
    </div>
    <div>
      <button
        class="rounded bg-pulse-green-500 px-3 py-1 text-white hover:bg-pulse-green-700"
        id="save-factsheet-link"
      >Save</button>
      <button
        class="rounded bg-red-500 px-3 py-1 text-white hover:bg-red-700"
        id="cancel-factsheet-link"
      >Cancel</button>
    </div>
  </div>
</div>

<script src="/assets/js/factsheet-link.js"></script>